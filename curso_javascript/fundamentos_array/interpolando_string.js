let nome = "José"
let poema = `
   E agora, ${nome}?
   A festa acabou,
   a luz apagou,
   o povo sumiu,
   a noite esfriou,
   e agora, ${nome}?
   e agora, você?
   você que é sem nome,
   que zomba dos outros,
   você que faz versos,
   que ama, protesta?
   e agora, ${nome}?
`
console.log(poema) // Imprime o poema
console.log(poema.length) // informa o tamanhp da string
console.log (typeof poema) // informa o tipo da variável